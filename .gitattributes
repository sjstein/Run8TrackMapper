# Keep line endings sane across Windows checkouts.
# Shell scripts MUST stay LF - Git Bash chokes on CRLF ("$'\r': command not found").
*.sh text eol=lf
# Windows batch files want CRLF.
*.bat text eol=crlf
