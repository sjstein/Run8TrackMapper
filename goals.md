# Current goal : Modify output_generator.py tool as follows:

## Add a search option to show industries based on a local tag. 


### Within the Config.ind file, there is a field for each industry which names the train ("local") which services it. 

Within the search feature, add a "Search by local symbol" which will present a drop-down list of all the local train 
symbols. When one is selected, Highlight only those industries which that local services. At the top of that local
search, also add a "<ALL>" entry to go back to the default industry display of showing them all (our current action).